# Copy this to .env and fill in real values (never commit .env to git)

OPENAI_API_KEY=sk-...

# JWT signing secret — change before deploying
SECRET_KEY=alliance-rag-secret-key-change-me

# Shared password for the /admin panel
ADMIN_PASSWORD=alliance2024

# Comma-separated list of allowed admin emails (informational, not enforced by auth)
ALLOWED_EMAILS=your@email.com

# Render overrides these automatically via render.yaml
# CHROMA_DB_PATH=/data/chroma_multi_store
# UPLOAD_DIR=/data/uploads
